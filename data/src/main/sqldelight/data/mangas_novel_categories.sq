CREATE TABLE mangas_novel_categories(
    _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    manga_id INTEGER NOT NULL,
    novel_category_id INTEGER NOT NULL,
    FOREIGN KEY(novel_category_id) REFERENCES novel_categories (_id)
    ON DELETE CASCADE,
    FOREIGN KEY(manga_id) REFERENCES mangas (_id)
    ON DELETE CASCADE
);

insert:
INSERT INTO mangas_novel_categories(manga_id, novel_category_id)
VALUES (:mangaId, :categoryId);

deleteNovelCategoryByMangaId:
DELETE FROM mangas_novel_categories
WHERE manga_id = :mangaId;
